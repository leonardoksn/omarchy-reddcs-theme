This is a Waybar theme. The files live here (not the usual Waybar config location) and get moved into place during installation.

All CSS must be GTK-compatible. Avoid properties that GTK CSS does not support (e.g., `line-height` and similar non-GTK features).
